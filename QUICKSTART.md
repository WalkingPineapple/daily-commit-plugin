# 快速开始指南

## 🚀 5分钟快速构建和测试

### 前置条件

- [x] IntelliJ IDEA 2023.1 或更高版本
- [x] JDK 17 或更高版本

### 步骤 1：打开项目

1. 启动 IntelliJ IDEA
2. 选择 `File` -> `Open`
3. 导航到：`E:\BBK\daily-commit-plugin`
4. 点击 `OK`

### 步骤 2：等待 Gradle 同步

IDEA 会自动：
- 下载 Gradle Wrapper
- 下载项目依赖
- 索引代码

⏱️ **预计时间：3-5分钟**（取决于网络速度）

### 步骤 3：构建插件

#### 方法 A：使用 Gradle 工具窗口（推荐）

1. 打开右侧的 `Gradle` 工具窗口
2. 展开：`daily-commit-plugin` -> `Tasks` -> `intellij`
3. 双击：`buildPlugin`

#### 方法 B：使用菜单

1. 选择：`Build` -> `Build Project`
2. 或按快捷键：`Ctrl+F9` (Windows/Linux) 或 `Cmd+F9` (Mac)

### 步骤 4：运行测试 IDE

在 Gradle 工具窗口中：
1. 展开：`Tasks` -> `intellij`
2. 双击：`runIde`

这会启动一个带有你的插件的测试 IDEA 实例。

### 步骤 5：安装插件（可选）

构建成功后，插件 ZIP 文件位于：
```
build/distributions/daily-commit-plugin-1.0.0.zip
```

安装到其他 IDEA 实例：
1. `Settings` -> `Plugins` -> `⚙️` -> `Install Plugin from Disk...`
2. 选择上述 ZIP 文件
3. 重启 IDEA

## 🧪 测试功能

### 1. 配置 LLM API

1. 在测试 IDE 中：`Settings` -> `Tools` -> `Daily Commit Manager`
2. 选择 LLM 提供商（例如 DeepSeek）
3. 填写 API Key 和其他配置
4. 点击 `Apply` 和 `OK`

### 2. 测试强制提交

1. 打开一个 Git 项目
2. 修改一些文件但不提交
3. 关闭并重新打开项目
4. 应该会看到强制提交对话框

### 3. 测试 AI 生成 Commit Message

1. 在强制提交对话框中
2. 点击 "AI 生成 Commit Message"
3. 等待生成完成
4. 编辑消息（如需要）
5. 点击"提交"

### 4. 查看日总结

提交后会自动生成日总结，保存在：
```
.idea/daily-summaries/YYYY-MM-DD.txt
```

### 5. 手动生成周总结

1. 右键点击项目
2. `VCS` -> `Generate Weekly Summary`
3. 编辑生成的总结
4. 点击"保存"

## 📝 DeepSeek API 配置示例

```
LLM 提供商: DeepSeek
API Key: sk-your-api-key-here
API Base URL: https://api.deepseek.com/v1
模型名称: deepseek-chat
```

申请 API Key：https://platform.deepseek.com/

## ❓ 常见问题

### Q: Gradle 同步失败？
A: 检查网络连接，或使用国内镜像：
在 `build.gradle.kts` 的 `repositories` 中添加：
```kotlin
maven { url = uri("https://maven.aliyun.com/repository/public") }
```

### Q: 构建太慢？
A: 首次构建需要下载 IntelliJ Platform SDK（约 500MB），请耐心等待。

### Q: 如何调试插件？
A:
1. 在代码中设置断点
2. 右键点击 `runIde` 任务
3. 选择 `Debug 'runIde'`

## 🎯 下一步

- 阅读完整文档：`README.md`
- 查看部署报告：`DEPLOYMENT_REPORT.md`
- 开始使用插件管理你的每日提交！

---

**祝你使用愉快！** 🎉
